{
  "_comments": {
    "usage": "Production loads secrets from Key Vault cmckv. Never commit real passwords.",
    "appService": "Use Key Vault references already configured on App Service",
    "local": "az/VS sign-in then: dotnet run --launch-profile azure",
    "setSecrets": "powershell -File scripts/set-keyvault-secrets.ps1 -SqlPassword '<password>' -PlatformSalt '<salt>'"
  },
  "KeyVault": {
    "VaultUri": "https://cmckv.vault.azure.net/"
  },
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "YOUR_ENTRA_TENANT_ID",
    "ClientId": "YOUR_API_APP_REGISTRATION_CLIENT_ID",
    "Audience": "api://YOUR_API_APP_REGISTRATION_CLIENT_ID"
  },
  "LocalDevelopment": {
    "UseDevAuth": false,
    "EnableRls": true
  }
}
